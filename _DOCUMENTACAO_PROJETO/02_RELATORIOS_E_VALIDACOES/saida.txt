  ⚡AUTOMAÇÃO PÓS-PAGAMENTOFluxo Completo: Asaas → Contrato → Login → Onboard → Monitoramento5 etapas 100% automáticas disparadas pelo webhook de pagamento confirmadoÍNDICE1. Visão Geral do Fluxo e Arquitetura2. Webhook Asaas — Recebimento e Validação3. Etapa 1 — Criação de Login e Senha Provisória4. Etapa 2 — Criação da Recorrência Mensal (12 meses)5. Etapa 3 — Geração e Envio do Contrato via Assinafy6. Etapa 4 — E-mail de Boas-vindas + Agendamento Cal.com7. Etapa 5 — Formulário Seguro de Chaves de API8. Pipeline Interno de Monitoramento de Clientes9. Notificações para o CEO (Telegram + E-mail)10. Armazenamento de Contratos Assinados11. Segurança das Credenciais dos Clientes12. Tecnologias e Dependências13. Checklist de Desenvolvimento1. Visão Geral do Fluxo e Arquitetura Todo o fluxo é disparado por um único evento: o webhook payment_confirmed do Asaas. A partir dali, 5 tarefas são executadas em sequência (ou parcialmente em paralelo) sem nenhuma intervenção manual. // FLUXO COMPLETO — EVENTO ÚNICO DISPARA TUDOASAAS: payment.confirmed  (webhook POST → /api/webhooks/asaas)           │           ├──[1]── Criar login (Supabase Auth) + senha provisória           │              └── Salvar hash da senha temporária           │           ├──[2]── Criar cobrança recorrente no Asaas           │              └── 12 parcelas, 1ª vence em 30 dias           │           ├──[3]── Gerar PDF do contrato + enviar via Assinafy           │              └── Webhook Assinafy → download PDF → Supabase Storage           │           ├──[4]── Enviar e-mail de boas-vindas           │              └── Link Cal.com + instruções + aviso do contrato           │           └──[5]── Criar registro no pipeline interno                          └── Status: PAGAMENTO_CONFIRMADO// Eventos subsequentes atualizam o pipeline:Assinafy: document.signed  → pipeline.status = CONTRATO_ASSINADOCal.com: booking.created   → pipeline.status = ONBOARD_AGENDADOForm: api_keys_submitted   → pipeline.status = CHAVES_RECEBIDASManual: onboard_realizado  → pipeline.status = ONBOARD_CONCLUIDO 🔐  Premissa de Segurança do FluxoNenhuma credencial de cliente (API keys de portais, tokens de redes sociais, etc.) trafega por e-mail.O formulário de chaves usa HTTPS + criptografia AES-256 antes de salvar no banco.Apenas os e-mails ceo@ e setup@ têm permissão de visualização dessas credenciais no painel.O contrato assinado fica armazenado no Supabase Storage com bucket privado, não acessível publicamente.A senha provisória do cliente é gerada com crypto.randomBytes(16), expirada em 24h, e nunca logada.2. Webhook Asaas — Recebimento e Validação O Asaas envia um POST para o endpoint de webhook sempre que um pagamento é confirmado. O sistema deve validar a autenticidade, extrair os dados do cliente e disparar o pipeline. 2.1 Configuração do Webhook no AsaasCampoConfiguraçãoURL do webhookhttps://seudominio.com.br/api/webhooks/asaasEvento a escutarPAYMENT_RECEIVED (ou PAYMENT_CONFIRMED para boleto)Token de autenticaçãoHeader: asaas-access-token = {seu token de API}Timeout de respostaResponder HTTP 200 em menos de 5 segundos (processar async)Retry em caso de falhaAsaas retenta por até 24h com backoff exponencial 2.2 Payload do Webhook Asaas// POST /api/webhooks/asaas// Body recebido do Asaas:{  &quot;event&quot;: &quot;PAYMENT_RECEIVED&quot;,  &quot;payment&quot;: {    &quot;id&quot;: &quot;pay_xxxxxxxxxx&quot;,    &quot;customer&quot;: &quot;cus_xxxxxxxxxx&quot;,    &quot;value&quot;: 297.00,    &quot;status&quot;: &quot;RECEIVED&quot;,    &quot;description&quot;: &quot;Contratação Sistema Imobiliário - Plano Pro&quot;,    &quot;billingType&quot;: &quot;CREDIT_CARD&quot;,  // ou BOLETO, PIX    &quot;confirmedDate&quot;: &quot;2024-03-15&quot;  }}// Buscar dados completos do cliente:// GET https://api.asaas.com/v3/customers/{customer_id}// Retorna: name, email, cpfCnpj, phone, address, mobilePhone // src/webhooks/asaas.webhook.tsexport async function handleAsaasPayment(req: Request, res: Response) {  // 1. Responder 200 IMEDIATAMENTE (Asaas não aguarda processamento)  res.status(200).json({ received: true });  // 2. Validar autenticidade  const token = req.headers[&apos;asaas-access-token&apos;];  if (token !== process.env.ASAAS_WEBHOOK_TOKEN) return;  const { event, payment } = req.body;  if (event !== &apos;PAYMENT_RECEIVED&apos;) return;  // 3. Buscar dados completos do cliente  const customer = await asaas.getCustomer(payment.customer);  // 4. Verificar se já existe (idempotência)  const exists = await db.clients.findByEmail(customer.email);  if (exists?.status !== &apos;AGUARDANDO_PAGAMENTO&apos;) return;  // 5. Disparar pipeline (fila de jobs para não bloquear o webhook)  await queue.add(&apos;post-payment-pipeline&apos;, { customer, payment });}3. Etapa 1 — Criação de Login e Senha Provisória 1Login e Acesso ProvisórioSupabase Auth · E-mail + senha temporária de 24h 3.1 Regras da Senha ProvisóriaRegraDetalheFormato12 caracteres: 4 letras maiúsculas + 4 números + 4 caracteres especiaisGeraçãocrypto.randomBytes(16) + base64 + sanitizaçãoExpiração24 horas após geração — forçar troca no primeiro acessoArmazenamentoNUNCA salvar a senha em texto puro — apenas o hash bcrypt(12 rounds)EnvioEnviada por e-mail transacional (Resend) criptografado — nunca por SMS ou TelegramTentativasBloquear acesso após 5 tentativas incorretas — exigir reset por e-mail // src/jobs/create-login.job.tsimport crypto from &apos;crypto&apos;;import { createClient } from &apos;@supabase/supabase-js&apos;;export async function createProvisionalLogin(customer: AsaasCustomer) {  // Gerar senha provisória segura  const rawPassword = generateSecurePassword(); // crypto.randomBytes  // Criar usuário no Supabase Auth  const { data: user, error } = await supabaseAdmin.auth.admin.createUser({    email: customer.email,    password: rawPassword,    email_confirm: true,  // confirmar e-mail automaticamente    user_metadata: {      name: customer.name,      phone: customer.mobilePhone,      tenant_slug: slugify(customer.name),      role: &apos;gestor&apos;,      provisional_password: true,  // forçar troca no 1º login      password_expires_at: new Date(Date.now() + 24 * 3600 * 1000).toISOString(),    }  });  // Salvar no banco de clientes  await db.clients.update(customer.email, {    supabase_user_id: user.id,    status: &apos;LOGIN_CRIADO&apos;,  });  // Retornar APENAS para o job de e-mail — nunca logar no console  return { userId: user.id, rawPassword };}4. Etapa 2 — Criação da Recorrência Mensal 2Recorrência de 12 MesesAsaas API · Cobrança mensal automática · 1º vencimento em 30 dias 4.1 Configuração da Assinatura RecorrenteParâmetroConfiguraçãocycleMONTHLY — cobrança mensalnextDueDatetoday + 30 dias (ISO: YYYY-MM-DD)totalValueValor anual = mensalidade × 12maxInstallments12 (cobranças geradas automaticamente)billingTypeCREDIT_CARD ou BOLETO (mesmo método do pagamento inicial)descriptionMensalidade Sistema Imobiliário — {NomeImobiliaria}Fine/Jurosfine: 2% + interest: 1% ao mês (configurável)NotificaçãosendPaymentByPostalService: false / notificationEnabled: true // src/jobs/create-subscription.job.tsexport async function createRecurringSubscription(  customerId: string,  planValue: number,  billingType: &apos;CREDIT_CARD&apos; | &apos;BOLETO&apos; | &apos;PIX&apos;) {  const nextDue = new Date();  nextDue.setDate(nextDue.getDate() + 30);  const subscription = await asaas.post(&apos;/subscriptions&apos;, {    customer: customerId,    billingType,    value: planValue,    nextDueDate: nextDue.toISOString().split(&apos;T&apos;)[0],    cycle: &apos;MONTHLY&apos;,    maxPayments: 12,    description: `Mensalidade Sistema Imobiliário - ${new Date().getFullYear()}`,    fine: { value: 2 },    interest: { value: 1 },    notificationEnabled: true,  });  await db.clients.update(customerId, {    asaas_subscription_id: subscription.id,    subscription_start: new Date(),    subscription_end: new Date(Date.now() + 365 * 24 * 3600 * 1000),    status: &apos;RECORRENCIA_CRIADA&apos;,  });  return subscription;} ⚠️  Atenção: Cobrança Inicial vs. RecorrênciaO webhook payment.confirmed refere-se à PRIMEIRA cobrança (ativação do serviço).A recorrência começa APÓS esta primeira cobrança — o cliente já pagou o primeiro mês.Portanto: 1ª cobrança manual (já feita) + 12 cobranças automáticas = 13 meses no total.Se o plano for anual com entrada, ajuste nextDueDate para + 30 dias conforme acordado com o cliente.Webhook a escutar para inadimplência: PAYMENT_OVERDUE → notificar CEO e bloquear acesso.5. Etapa 3 — Geração e Envio do Contrato via Assinafy 3Contrato Digital AssinafyLogin automático → Upload PDF → Envio ao cliente → Webhook de assinatura → Download + Armazenamento 5.1 Fluxo Completo da Assinafy#AçãoAPI Call1Login na AssinafyPOST https://api.assinafy.com.br/login2Gerar PDF do contratoPreencher template com dados do cliente (PDFKit ou Puppeteer)3Upload e criação do documentoPOST /documents (multipart/form-data com o PDF)4Adicionar signatáriosSua assinatura (já configurada) + e-mail do cliente5Enviar para assinaturaAssinafy envia link por e-mail automaticamente6Aguardar webhookPOST /webhooks/assinafy/signed → event: document.signed7Download do PDF assinadoGET /documents/{document_id}/download8ArmazenarUpload para Supabase Storage (bucket contratos-assinados, privado) 5.2 Implementação — Login e Upload// src/jobs/generate-contract.job.tsconst ASSINAFY_BASE = &apos;https://api.assinafy.com.br&apos;;// PASSO 1: Autenticar na Assinafy (token expira — fazer login a cada chamada)async function getAssinafyToken(): Promise&lt;string&gt; {  const res = await axios.post(`${ASSINAFY_BASE}/login`, {    email: process.env.ASSINAFY_EMAIL,    password: process.env.ASSINAFY_PASSWORD,  });  return res.data.token;  // Bearer token}// PASSO 2: Gerar PDF preenchido com dados do clienteasync function generateContractPDF(client: ClientData): Promise&lt;Buffer&gt; {  // Usar Puppeteer: renderizar HTML do contrato como PDF  // OU usar pdf-lib para preencher campos de um PDF template  const html = contractTemplate(client);  // sua função de template  const pdf = await htmlToPdf(html);      // Puppeteer headless  return pdf;}// PASSO 3: Upload do documento para Assinafyasync function uploadContractToAssinafy(  pdfBuffer: Buffer,  client: ClientData,  token: string): Promise&lt;string&gt; {  const formData = new FormData();  formData.append(&apos;file&apos;, pdfBuffer, `contrato_${client.slug}.pdf`);  formData.append(&apos;name&apos;, `Contrato - ${client.name} - ${new Date().toLocaleDateString(&apos;pt-BR&apos;)}`);  // Signatários: você (já tem assinatura salva) + cliente  formData.append(&apos;signers&apos;, JSON.stringify([    { email: process.env.CEO_EMAIL, name: &apos;CEO&apos;, role: &apos;sign&apos; },    { email: client.email, name: client.name, role: &apos;sign&apos; },  ]));  const res = await axios.post(`${ASSINAFY_BASE}/documents`, formData, {    headers: { Authorization: `Bearer ${token}`, ...formData.getHeaders() }  });  return res.data.id;  // document_id} 5.3 Webhook de Assinatura Concluída// POST /api/webhooks/assinafy// Disparado pela Assinafy quando TODOS os signatários assinaramexport async function handleAssinafySigned(req: Request, res: Response) {  res.status(200).json({ ok: true });  const { event, document_id } = req.body;  if (event !== &apos;document.signed&apos;) return;  // 1. Buscar token Assinafy  const token = await getAssinafyToken();  // 2. Download do PDF assinado  const pdfResponse = await axios.get(    `${ASSINAFY_BASE}/documents/${document_id}/download`,    { headers: { Authorization: `Bearer ${token}` }, responseType: &apos;arraybuffer&apos; }  );  // 3. Upload para Supabase Storage (bucket PRIVADO)  const client = await db.clients.findByDocumentId(document_id);  const path = `contratos/${client.slug}/${document_id}_signed.pdf`;  await supabase.storage    .from(&apos;contratos-assinados&apos;)  // bucket com acesso privado    .upload(path, pdfResponse.data, { contentType: &apos;application/pdf&apos; });  // 4. Atualizar pipeline  await db.clients.update(client.id, {    contract_signed_at: new Date(),    contract_storage_path: path,    status: &apos;CONTRATO_ASSINADO&apos;,  });  // 5. Desbloquear agendamento no sistema  await sendOnboardUnlockedEmail(client);} 📋  Template do Contrato — Campos ObrigatóriosO PDF do contrato deve ser gerado com os seguintes dados preenchidos automaticamente:  • Razão social / Nome completo do cliente  • CPF/CNPJ  • Endereço completo (dados do Asaas)  • Valor do plano contratado e forma de pagamento  • Data de início do contrato e vigência (12 meses)  • Data da primeira cobrança recorrente  • Cláusulas de SLA, LGPD e confidencialidade  • Campo de assinatura digital (preenchido automaticamente pela Assinafy)Recomendação: manter o template em HTML e converter com Puppeteer — mais fácil de manter.6. Etapa 4 — E-mail de Boas-vindas + Agendamento Cal.com 4E-mail de Boas-Vindas + AgendamentoResend API · Cal.com · Notificação Telegram ao CEO 6.1 Estrutura do E-mail de Boas-VindasSeção do E-mailConteúdoCabeçalhoLogo + &apos;Bem-vindo(a), [Nome]! Sua conta foi criada.&apos;Credenciais de AcessoE-mail de login + senha provisória + link do painel + aviso de expiração 24hStatus do Contrato⏳ Seu contrato foi enviado para assinatura. O agendamento do onboard será liberado após a assinatura.Instruções de API KeysAviso de que após assinar será enviado formulário seguro para as chaves de integraçãoPróximos Passos1. Assine o contrato (link em outro e-mail da Assinafy) → 2. Receba o link de agendamento → 3. Agende seu onboardContato de SuporteE-mail e WhatsApp para dúvidas urgentes // src/jobs/send-welcome-email.job.ts// Usar Resend (resend.com) — API simples, boa entregabilidadeexport async function sendWelcomeEmail(client: ClientData, password: string) {  await resend.emails.send({    from: &apos;Bem-vindo &lt;noreply@seudominio.com.br&gt;&apos;,    to: client.email,    subject: `🎉 Sua conta está pronta, ${client.name.split(&apos; &apos;)[0]}!`,    html: welcomeEmailTemplate({      name: client.name,      email: client.email,      password,  // senha provisória (única vez que é enviada)      dashboardUrl: `https://app.seudominio.com.br`,      contractStatus: &apos;pending&apos;,  // contrato ainda não assinado    }),  });}// SEGUNDO E-MAIL: enviado APÓS assinatura do contrato (Assinafy webhook)export async function sendOnboardScheduleEmail(client: ClientData) {  const calLink = buildCalLink(client);  // Cal.com com dados pré-preenchidos  await resend.emails.send({    from: &apos;Onboard &lt;onboard@seudominio.com.br&gt;&apos;,    to: client.email,    subject: &apos;✅ Contrato assinado! Agende seu Onboard&apos;,    html: onboardEmailTemplate({      name: client.name,      calLink,      apiKeysFormUrl: `https://setup.seudominio.com.br/chaves?token=${client.secureFormToken}`,    }),  });} 6.2 Integração Cal.com — Agendamento do OnboardConfiguraçãoDetalheEvento Cal.comCriar evento &apos;Onboard Sistema Imobiliário&apos; (60 min)Link personalizadohttps://cal.com/seuname/onboard?name={nome}&amp;email={email}Webhook Cal.comPOST /api/webhooks/calcom → event: BOOKING_CREATEDAção no webhookAtualizar status do cliente para ONBOARD_AGENDADONotificação CEOTelegram: &apos;Onboard agendado: [Nome] — [Data/Hora]&apos;Notificação CEO e-mailResumo do cliente: nome, empresa, plano, data do onboardLembrete automáticoCal.com envia lembrete 24h e 1h antes automaticamente // Webhook Cal.com: POST /api/webhooks/calcomexport async function handleCalcomBooking(req: Request, res: Response) {  res.status(200).json({ ok: true });  const { triggerEvent, payload } = req.body;  if (triggerEvent !== &apos;BOOKING_CREATED&apos;) return;  const clientEmail = payload.attendees[0].email;  const client = await db.clients.findByEmail(clientEmail);  // Atualizar pipeline  await db.clients.update(client.id, {    onboard_scheduled_at: payload.startTime,    status: &apos;ONBOARD_AGENDADO&apos;,  });  // Notificar CEO via Telegram  await sendTelegramMessage(process.env.CEO_TELEGRAM_CHAT_ID,    `📅 ONBOARD AGENDADO\n` +    `Cliente: ${client.name}\n` +    `Empresa: ${client.company}\n` +    `Data: ${formatDate(payload.startTime)}\n` +    `Plano: ${client.plan}`  );  // Notificar CEO por e-mail  await sendCeoEmail(&apos;onboard_scheduled&apos;, client, payload);}7. Etapa 5 — Formulário Seguro de Chaves de API 5Formulário Ultra-Seguro de API KeysToken único por cliente · HTTPS · AES-256 · Acesso restrito a 2 e-mails Este é o componente mais crítico do sistema do ponto de vista de segurança. As chaves de API dos clientes (Instagram, OLX, Asaas deles, etc.) são informações altamente sensíveis que dão acesso às contas comerciais deles. 7.1 Arquitetura de Segurança do Formulário// PRINCÍPIOS DE SEGURANÇA:// 1. URL com token único por cliente — expira em 72h//    https://setup.seudominio.com.br/chaves?token=jwt_unico_temporario// 2. O token é um JWT assinado com segredo diferente da aplicação principal//    Payload: { clientId, email, exp: +72h, purpose: &apos;api-keys-form&apos; }// 3. HTTPS obrigatório — nunca aceitar HTTP// 4. Cada campo é criptografado ANTES de sair do browser//    (Web Crypto API — AES-GCM 256 bits)// 5. O servidor recebe os dados JÁ CRIPTOGRAFADOS//    Descriptografa com chave que NUNCA vai ao frontend// 6. Banco: coluna jsonb &apos;api_credentials&apos; criptografada//    com pgcrypto (Supabase extension)// 7. RLS: apenas service_role pode ler api_credentials//    Nem o próprio gestor da imobiliária vê os dados 7.2 Campos do Formulário de ChavesIntegraçãoCampoObservação para o ClienteWhatsApp BusinessPhone Number ID + Token de AcessoMeta for Developers → App → WhatsApp → API SetupInstagram / FacebookApp ID + App Secret + Page TokenMesma conta Meta Business — permissões pages_messagingOLX ImóveisWebhook Secret TokenOLX Painel Anunciante → Configurações → APIZAP ImóveisAPI Token ZAPZAP Central → Minha Conta → IntegraçõesViva RealAPI Key Viva RealPainel Anunciante → Configurações → APITelegram BotBot Token@BotFather no Telegram → /newbot → copiar tokenTikTok BusinessApp ID + App SecretTikTok for Business → Developers → My AppsAsaas (da imobiliária)API Key (sandbox ou produção)Asaas → Minha Conta → Integrações → Chaves de API // src/app/chaves/page.tsx — Formulário com criptografia client-side// Antes de enviar ao servidor, criptografar cada campo:async function encryptField(value: string, publicKey: CryptoKey): Promise&lt;string&gt; {  const encoded = new TextEncoder().encode(value);  const iv = crypto.getRandomValues(new Uint8Array(12));  const encrypted = await crypto.subtle.encrypt(    { name: &apos;AES-GCM&apos;, iv },    publicKey,    encoded  );  return btoa(JSON.stringify({    iv: Array.from(iv),    data: Array.from(new Uint8Array(encrypted))  }));}// POST /api/client-keys (autenticado com o JWT do formulário)// Body: { clientId, encryptedKeys: { whatsapp: &apos;...&apos;, instagram: &apos;...&apos;, ... } }// Servidor descriptografa e salva no banco com pgcrypto:// UPDATE clients SET api_credentials = pgp_sym_encrypt(data, KEY) WHERE id = clientId 7.3 Controle de Acesso às CredenciaisQuemAcessoCEO (ceo@seudominio.com.br)Visualização completa no painel admin — descriptografa na telaSetup (setup@seudominio.com.br)Visualização completa — acesso para configurar o sistemaOutros usuários internosSEM ACESSO — RLS bloqueia no bancoO próprio cliente (gestor da imobiliária)SEM ACESSO — eles fornecem mas não visualizam após o envioDesenvolvedores / logsPROIBIDO logar API keys — código auditado para garantir issoBackupBackup criptografado — chave de descriptografia separada do backup 🛡️  Instruções de Segurança para o Cliente (incluir no e-mail)Este formulário é de uso único e expira em 72 horas.O link é pessoal e intransferível — não compartilhe com ninguém.Nenhum funcionário nosso solicitará suas chaves por e-mail ou telefone.Suas credenciais são criptografadas antes mesmo de deixar seu computador.Após o envio, você não poderá visualizar as chaves novamente por este link.Em caso de suspeita de vazamento, entre em contato imediatamente para revogarmos e recadastrarmos.8. Pipeline Interno de Monitoramento de Clientes O pipeline interno é uma tabela de estados que representa exatamente em que etapa cada cliente está. É o coração do painel de monitoramento que você usará para acompanhar todos os clientes em tempo real. 8.1 Estados do PipelineStatusGatilhoPróxima Ação EsperadaLEADCadastro manual ou formulário de interesseEnviar proposta comercialPROPOSTA_ENVIADAEnvio da propostaAguardar pagamentoAGUARDANDO_PAGAMENTOLink de pagamento gerado no AsaasWebhook PAYMENT_RECEIVEDPAGAMENTO_CONFIRMADOWebhook Asaas — job disparadoAssinar contratoLOGIN_CRIADOSupabase Auth criadoAssinar contratoRECORRENCIA_CRIADASubscription Asaas criadaAssinar contratoCONTRATO_ENVIADODocumento enviado via AssinafyCliente assinar contratoCONTRATO_ASSINADOWebhook Assinafy document.signedAgendar onboardONBOARD_AGENDADOWebhook Cal.com BOOKING_CREATEDReceber API keysCHAVES_RECEBIDASForm de API keys submetidoRealizar onboardONBOARD_CONCLUIDOMarcação manual pelo CEO/setupProvisionar sistemaPROVISIONANDOJob de infra iniciadoAguardar conclusãoATIVOProvisionamento concluídoAcompanhamento e suporteINADIMPLENTEWebhook PAYMENT_OVERDUE (Asaas)Notificar e tentar recuperarCHURNEDCancelamento — marcação manualAnálise de churn 8.2 Estrutura da Tabela no Banco-- Banco MASTER: tabela central de pipelineCREATE TABLE client_pipeline (  id                    UUID PRIMARY KEY DEFAULT gen_random_uuid(),  -- Identificação  name                  TEXT NOT NULL,  company_name          TEXT,  email                 TEXT UNIQUE NOT NULL,  phone                 TEXT,  -- Asaas  asaas_customer_id     TEXT,  asaas_payment_id      TEXT,  asaas_subscription_id TEXT,  plan_value            DECIMAL(10,2),  -- Acesso  supabase_user_id      UUID,  tenant_slug           TEXT,  -- Contrato  assinafy_document_id  TEXT,  contract_sent_at      TIMESTAMPTZ,  contract_signed_at    TIMESTAMPTZ,  contract_storage_path TEXT,  -- Onboard  onboard_scheduled_at  TIMESTAMPTZ,  onboard_completed_at  TIMESTAMPTZ,  api_keys_received_at  TIMESTAMPTZ,  -- Pipeline  status                TEXT NOT NULL DEFAULT &apos;LEAD&apos;,  status_updated_at     TIMESTAMPTZ DEFAULT NOW(),  notes                 TEXT,  -- anotações manuais do CEO  -- Datas  created_at            TIMESTAMPTZ DEFAULT NOW(),  updated_at            TIMESTAMPTZ DEFAULT NOW());-- Tabela de histórico de mudanças de statusCREATE TABLE pipeline_history (  id          UUID PRIMARY KEY DEFAULT gen_random_uuid(),  client_id   UUID REFERENCES client_pipeline(id),  from_status TEXT,  to_status   TEXT,  changed_by  TEXT,  -- &apos;system&apos; ou email do usuário  notes       TEXT,  changed_at  TIMESTAMPTZ DEFAULT NOW());9. Notificações para o CEO — Telegram + E-mail 9.1 Todos os Eventos que Notificam o CEOEventoCanalConteúdo da NotificaçãoNovo pagamento confirmadoTelegram + E-mailNome, empresa, plano, valor, forma de pagamentoContrato assinadoTelegramNome, empresa — &apos;Pronto para agendar onboard&apos;Onboard agendadoTelegram + E-mailNome, data/hora, link da reunião Cal.comAPI Keys recebidasTelegramNome — &apos;Chaves recebidas, pronto para provisionar&apos;Inadimplência detectadaTelegram + E-mailNome, valor em atraso, dias de atrasoContrato NÃO assinado em 72hTelegramAlerta: cliente não assinou — follow-up necessárioOnboard não agendado em 5 diasTelegramAlerta: cliente não agendou — follow-up necessário 9.2 Configuração do Bot Telegram// 1. Criar bot via @BotFather no Telegram//    /newbot → nome → username → copiar BOT_TOKEN// 2. Obter chat_id do CEO://    a) Enviar qualquer mensagem para o bot//    b) GET https://api.telegram.org/bot{TOKEN}/getUpdates//    c) message.chat.id = seu chat_id// 3. Enviar mensagem:async function sendTelegramMessage(chatId: string, text: string) {  await axios.post(    `https://api.telegram.org/bot${process.env.TELEGRAM_BOT_TOKEN}/sendMessage`,    {      chat_id: chatId,      text,      parse_mode: &apos;Markdown&apos;,  // permite **negrito** e _itálico_    }  );}// Exemplo de mensagem formatada:const msg = `💰 *NOVO PAGAMENTO CONFIRMADO*Cliente: ${name}Empresa: ${company}Plano: ${plan} — R$ ${value}Forma: ${billingType}`,10. Armazenamento de Contratos Assinados O Assinafy não possui banco de dados persistente — o PDF assinado deve ser baixado via API imediatamente após o webhook de assinatura e armazenado no seu próprio sistema. AspectoSoluçãoOnde armazenarSupabase Storage — bucket &apos;contratos-assinados&apos; com acesso PRIVADONomenclatura do arquivo{tenant_slug}/{ano}/{document_id}_signed_{timestamp}.pdfAcesso ao PDFURL assinada temporária (Supabase signed URL) — expira em 1 horaQuem pode gerar a URLApenas service_role — nem o gestor da imobiliária acessa diretamenteBackupReplicação automática do Supabase Storage (configurar redundância)RetençãoIndefinida — contrato é documento legal — nunca deletar automaticamenteMetadados no bancoclient_id, document_id, signed_at, storage_path, assinafy_document_id 📁  Hierarquia de Pastas no Supabase StorageBucket: contratos-assinados (PRIVADO — sem leitura pública)  contratos-assinados/    horizonte-imoveis/      2024/        doc_abc123_signed_20240315T143022.pdf    bela-vista-imoveis/      2024/        doc_xyz789_signed_20240318T091500.pdfAcesso: apenas via supabase.storage.createSignedUrl() com service_role keyNunca gerar URLs públicas para contratos.11. Segurança das Credenciais dos Clientes 11.1 Variáveis de Ambiente NecessáriasVariávelDescrição e ObservaçãoASAAS_API_KEYSua API key do Asaas — NUNCA expor no frontendASAAS_WEBHOOK_TOKENToken de verificação dos webhooks do AsaasASSINAFY_EMAIL / ASSINAFY_PASSWORDCredenciais de login na API AssinafyENCRYPTION_KEY_API_CREDSChave AES-256 para criptografar API keys dos clientes (32 bytes)JWT_SECRET_SECURE_FORMSegredo exclusivo para tokens do formulário de chavesTELEGRAM_BOT_TOKENToken do bot criado via @BotFatherCEO_TELEGRAM_CHAT_IDChat ID do Telegram do CEOCEO_EMAIL / SETUP_EMAILE-mails autorizados a ver credenciais dos clientesSUPABASE_SERVICE_ROLE_KEYKey de serviço — NUNCA expor, apenas no backendRESEND_API_KEYAPI key do Resend para envio de e-mails transacionaisCALCOM_WEBHOOK_SECRETSecret para verificar webhooks do Cal.com ⚠️  Regras Absolutas de Segurança1. NUNCA logar API keys de clientes — auditar todo o código para garantir.2. NUNCA enviar API keys por e-mail, Telegram ou qualquer canal não-criptografado.3. NUNCA armazenar senhas em texto puro — apenas hash bcrypt com 12 rounds.4. NUNCA expor a SUPABASE_SERVICE_ROLE_KEY no frontend ou em logs.5. NUNCA gerar URLs públicas para contratos — sempre usar signed URLs com expiração.6. Rotação de chaves: ENCRYPTION_KEY_API_CREDS deve ser rotacionada a cada 6 meses.7. Auditoria: manter log de quem acessou credenciais (ceo@ ou setup@) com timestamp.8. Formulário de chaves: invalidar token IMEDIATAMENTE após o primeiro submit.12. Tecnologias e Dependências Tecnologia / ServiçoFunção no FluxoInstalação / ConfiguraçãoAsaasPagamento, assinatura recorrente, webhooksConta já existente — configurar webhook URLAssinafyContrato digital, assinatura eletrônicaConta existente — API via login/tokenSupabaseBanco de dados, Auth, Storage, Realtimesupabase-js + service_role para backendResendE-mails transacionaisnpm install resend — API key no painelCal.comAgendamento do onboardConta + evento + webhook configuradoTelegram Bot APINotificações ao CEOBot criado via @BotFatherBullMQ + RedisFila de jobs assíncronosnpm install bullmq — Redis no Railway/UpstashPuppeteerGeração do PDF do contratonpm install puppeteer — render HTML → PDFCloudflare R2Storage de documentos de imóveisConta Cloudflare existentedate-fnsManipulação de datas (30 dias, expiração)npm install date-fnszodValidação dos payloads dos webhooksnpm install zod13. Checklist de Desenvolvimento Sprint 1 — Webhook e Login#TarefaCritério de Aceite1Configurar endpoint POST /api/webhooks/asaas com validação HMACWebhook recebido e validado — payload salvo no banco2Implementar fila BullMQ para jobs assíncronos do pipelineJob disparado em &lt; 2s após webhook, processamento assíncrono3Job: createProvisionalLogin (Supabase Auth)Usuário criado, senha temporária gerada, hash salvo4Job: createRecurringSubscription (Asaas API)Assinatura criada com 12 parcelas, 1ª vence em 30 dias5E-mail de boas-vindas com credenciais (Resend)E-mail entregue em &lt; 60s com senha provisória Sprint 2 — Contrato e Agendamento#TarefaCritério de Aceite6Template HTML do contrato com campos dinâmicosPDF gerado corretamente com todos os dados do cliente7Integração Assinafy: login, upload, signatários, envioDocumento criado na Assinafy e e-mail enviado ao cliente8Webhook Assinafy: download PDF assinado + upload Supabase StoragePDF salvo no bucket privado, path salvo no banco9E-mail pós-assinatura com link Cal.com personalizadoE-mail enviado em &lt; 60s após assinatura confirmada10Webhook Cal.com: atualizar status + notificar CEO (Telegram + e-mail)CEO notificado em &lt; 30s após agendamento Sprint 3 — Formulário de Chaves e Pipeline#TarefaCritério de Aceite11Token JWT de uso único para o formulário de chaves (expira em 72h)Token inválido após 1 uso ou após 72h12Frontend do formulário seguro com criptografia client-side (AES-GCM)Dados chegam criptografados ao servidor13Backend: descriptografia e armazenamento com pgcrypto no SupabaseAPI keys salvas criptografadas, apenas service_role lê14RLS: apenas CEO e setup acessam credenciais — testar com outros usuáriosOutros usuários recebem 403 ao tentar acessar15Painel interno de pipeline: Kanban com todos os clientes e statusAtualização em tempo real via Supabase Realtime16Alertas automáticos: contrato não assinado em 72h, onboard não agendado em 5 diasMensagem Telegram enviada corretamente nos prazos17Testes E2E do fluxo completo em ambiente de stagingCliente criado no Asaas sandbox → todos os 5 passos executados  🚀  Resultado: O que acontece nos primeiros 3 minutos após o pagamentoT+0s   → Webhook Asaas recebido, job enfileiradoT+5s   → Login criado no Supabase AuthT+10s  → Assinatura recorrente criada no AsaasT+20s  → PDF do contrato gerado e enviado via AssinafyT+30s  → E-mail de boas-vindas com credenciais enviado ao clienteT+30s  → CEO notificado via TelegramT+30s  → Pipeline atualizado: status = CONTRATO_ENVIADOApós assinatura do contrato (tempo variável — depende do cliente):         → PDF assinado baixado e armazenado         → E-mail com link de agendamento enviado         → Cliente acessa Cal.com e escolhe data/hora         → CEO notificado do agendamento (Telegram + e-mail)